# Optional import of local .env file — won't fail startup if .env is missing
spring.config.import=optional:file:.env[.properties]

spring.application.name=mental_health_consultation_sys

cors.allowed.origins=${REACT_APP_URL}

spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USERNAME}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Session timeout (e.g., 30 minutes)
server.servlet.session.timeout=1800

logging.level.org.springframework.http.converter.json=DEBUG
logging.level.com.fasterxml.jackson=DEBUG

# JWT secret can be provided via environment variable JWT_SECRET (default kept for backward-compatibility)
jwt.secret=${JWT_SECRET:19e5ad48a0b34cf915fbdbc337b87a9c77ef1806cad38a7d68489cb32d72565e}
jwt.expiration=86400000

# Twilio properties for OTP authentication — can be set via env vars
twilio.account.sid=${TWILIO_ACCOUNT_SID:ACbf70e09e11b2179b7808e8289e5c663e}
twilio.auth.token=${TWILIO_AUTH_TOKEN:2cccf44898f91657ff3590720f10bd7f}
twilio.service.sid=${TWILIO_SERVICE_SID:VA8830b100b910906dbad8bacf59ae25b0}